[pytest]
testpaths = tests
markers =
    fast: Fast tests (DB, API) - no LLM or embedding calls
    slow: Slow tests requiring LLM/embedding services
    pipeline: End-to-end RAG pipeline tests
asyncio_mode = auto
